/* Bruno Electric — Electrical Tasks Stage 7 advanced-template adapters. Reuses audited deterministic engines; no duplicate electrical math. */
(function(root){
'use strict';
var G=root.BrunoGroundingEngine;if(!G)throw new Error('Grounding engine must load before Advanced Electrical Tasks');var VERSION='electrical-task-advanced-v7';
function clone(x){return x==null?x:JSON.parse(JSON.stringify(x))}function warning(result,text){var r=clone(result);r.warnings=Array.isArray(r.warnings)?r.warnings.slice():[];if(r.warnings.indexOf(text)<0)r.warnings.unshift(text);r.taskTemplateEngineVersion=VERSION;return r}
function base(input,grounding,text){return warning(G.calculate(clone(input||{}),clone(grounding||{})),text)}
function branch(i,g){return base(i,g,'Branch Circuit Run uses the shared deterministic conductor / voltage-drop / raceway / EGC chain from explicit design-current inputs. Branch-specific load, OCPD, equipment and special-occupancy rules are not inferred; unresolved compliance facts require separate verification.')}
function evse(i,g){i=clone(i||{});if(i.loadBasis!=='CONTINUOUS')throw new Error('EVSE Circuit requires the explicit CONTINUOUS load basis in this template; the adapter will not silently change the user load basis.');return base(i,g,'EVSE Circuit uses the shared deterministic sizing chain with an explicit continuous-load basis. Equipment listing, adjustable-output settings, load-management controls, receptacle/GFCI details and site-specific EVSE requirements are not inferred.')}
function hvac(i,g){return base(i,g,'HVAC Circuit treats Load / Design Current as an explicit equipment-derived conductor design current and OCPD as an explicit equipment/design fact. This adapter does not derive MCA, MOCP, compressor rules, overload protection, disconnect requirements, or equipment listing instructions.')}
function motor(i,g){i=clone(i||{});if(i.loadBasis!=='NONCONTINUOUS')throw new Error('Motor Circuit requires NONCONTINUOUS because Load / Design Current must already be the motor conductor design current; the adapter will not apply a second generic factor.');return base(i,g,'Motor Circuit treats Load / Design Current as an explicit already-derived motor conductor design current and OCPD as an explicit motor-circuit protection fact. Motor-specific FLC, protection, overload and controller rules are not inferred.')}
function transformer(i,g){i=clone(i||{});if(i.loadBasis!=='NONCONTINUOUS')throw new Error('Transformer Feed requires NONCONTINUOUS because Load / Design Current must already be the selected transformer conductor design current; the adapter will not add a second generic multiplier.');return base(i,g,'Transformer Feed treats Load / Design Current as an explicit already-derived primary or secondary conductor design current and OCPD as an explicit protection/design fact. Transformer-specific protection, tap, fault-current and grounding rules are not inferred.')}
function generator(i,g){i=clone(i||{});if(i.loadBasis!=='NONCONTINUOUS')throw new Error('Generator / Feeder requires NONCONTINUOUS because Load / Design Current must already be the selected generator feeder conductor design current; the adapter will not add a second generic multiplier.');return base(i,g,'Generator / Feeder treats Load / Design Current as an explicit already-derived feeder conductor design current and OCPD as an explicit protection/design fact. Source-current derivation, transfer, standby classification, grounding and source-specific protection rules are not inferred.')}
function longRun(i,g){i=clone(i||{});if(i.loadBasis!=='CONTINUOUS'&&i.loadBasis!=='NONCONTINUOUS')throw new Error('Generic Long Run requires an explicit CONTINUOUS or NONCONTINUOUS load basis.');return base(i,g,'Generic Long Run uses the shared deterministic ampacity + resistance-only K voltage-drop + raceway + EGC chain from explicit design current, load basis and distance. Long distance does not bypass ampacity. Power-factor/reactance effects, load-specific rules and pulling feasibility are not inferred.')}
function calculate(t,i,g){if(t==='BRANCH_CIRCUIT_RUN')return branch(i,g);if(t==='EVSE_CIRCUIT')return evse(i,g);if(t==='HVAC_CIRCUIT')return hvac(i,g);if(t==='MOTOR_CIRCUIT')return motor(i,g);if(t==='TRANSFORMER_FEED')return transformer(i,g);if(t==='GENERATOR_FEEDER')return generator(i,g);if(t==='LONG_DISTANCE_VD')return longRun(i,g);throw new Error('Advanced task template is not enabled by this adapter')}
root.BrunoElectricalTaskAdvanced=Object.freeze({calculate:calculate,branch:branch,evse:evse,hvac:hvac,motor:motor,transformer:transformer,generator:generator,longRun:longRun,version:VERSION});
})(typeof window!=='undefined'?window:globalThis);
